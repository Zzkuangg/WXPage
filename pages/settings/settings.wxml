<!--pages/settings/settings.wxml-->
<view class="grid-list">
  <view class="grid-item" wx:for="{{gridList}}" wx:key="id" bindtap="goSets" data-id="{{item.id}}">
    <image src="{{item.icon}}"></image>
    <text>{{item.name}}</text>
  </view>
</view>
